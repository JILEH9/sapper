BOT_TOKEN=
TELEGRAM_API_URL=https://api.telegram.org

# Опционально. Если задан — users/updates в Postgres. Без него бот работает, ничего не сохраняет.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
